import React, { Component } from 'react';

class PageLoader extends Component {
    render() {
        return (
            <section className="pageLoader">page loader</section>
        );
    }
}

export { PageLoader };
